# Tool-Derived Entry Cards

Store one file per tool-derived candidate signal here.

Each card should include:

- tool name
- output artifact
- provenance
- what the output may suggest
- what the output does not justify claiming
